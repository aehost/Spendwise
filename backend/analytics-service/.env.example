# ── Supabase / PostgreSQL ─────────────────────────────────────
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres
DB_SSL=true

# ── JWT (must match api-gateway and auth-service) ─────────────
JWT_ACCESS_SECRET=replace_with_a_long_random_secret_min_32_chars

# ── CORS (internal service — gateway is the only caller) ──────
CORS_ORIGINS=*

# ── Optional: Google Places API for merchant classification ───
# Leave blank if not used — falls back gracefully to text-only classification
GOOGLE_PLACES_API_KEY=

# ── Server ────────────────────────────────────────────────────
PORT=3004
NODE_ENV=production
