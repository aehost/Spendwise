# API gateway URL (Railway deployment)
# In local dev: leave blank — Vite proxy handles /api → localhost:3000
# In production (Vercel): set to your Railway gateway public URL
VITE_API_URL=https://your-gateway.up.railway.app/api
